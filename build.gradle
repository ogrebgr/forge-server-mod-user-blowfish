group 'com.bolyartech.forge.server.module'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
    jcenter();
}

dependencies {
    compile project(':forge-server-mod-user')
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
